# Web app

The Next.js shell is intentionally minimal. It is not part of the verified offline core build because dependencies cannot be installed in the current execution environment.

Planned first pages:

1. Capture
2. Model Update Review
3. Model Detail / Evidence Ledger
4. Graph neighborhood inspection
